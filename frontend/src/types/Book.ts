export interface Book {
    bookID: number;
    publisher: ReactNode;
    isbn: ReactNode;
    classification: ReactNode;
    category: ReactNode;
    pageCount: ReactNode;
    id: number;
    title: string;
    author: string;
    price: number;
    description: string;
    imageUrl: string;
    stock: number;
}